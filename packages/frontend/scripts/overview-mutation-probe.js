export const mutationProbe =
  "(async()=>{\n if(location.hostname!=='localhost'||![4196,4198].includes(Number(location.port))||location.pathname!=='/orgs/measure/projects/ten-thousand/tickets/T-10000')throw Error('Use the isolated benchmark preview');\n const pause=ms=>new Promise(r=>setTimeout(r,ms));\n const wait=async fn=>{const start=performance.now();while(!fn()){if(performance.now()-start>10000)throw Error('Mutation timeout');await pause(5)}};\n const selector='button.text-2xl';\n await wait(()=>document.querySelector(selector));\n const original=document.querySelector(selector).textContent;\n const realFetch=globalThis.fetch, requests=[];\n globalThis.fetch=async(input,init)=>{const start=performance.now();const url=input instanceof Request?input.url:String(input);const method=init?.method??(input instanceof Request?input.method:'GET');const r=await realFetch(input,init);requests.push({url,method,start,end:performance.now(),status:r.status});return r};\n const results=[];\n try{\n for(let i=0;i<5;i++){\n  document.querySelector(selector).click();\n  await wait(()=>document.querySelector('input[maxlength=\"200\"]'));\n  const input=document.querySelector('input[maxlength=\"200\"]');\n  const title='Benchmark rerun title '+i+' '+location.port;\n  Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(input,title);\n  input.dispatchEvent(new Event('input',{bubbles:true}));\n  await pause(20);\n  requests.length=0;\n  const start=performance.now();\n  input.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',code:'Enter',bubbles:true}));\n  await wait(()=>document.querySelector(selector)?.textContent===title&&!document.querySelector('input[maxlength=\"200\"]'));\n  const ms=performance.now()-start;\n  await pause(100);\n  results.push({ms,requests:requests.map(r=>({...r,start:r.start-start,end:r.end-start}))});\n }\n return results;\n }finally{globalThis.fetch=realFetch;const r=await realFetch('/api/orgs/measure/projects/ten-thousand/tickets/T-10000',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({title:original})});if(!r.ok)throw Error('Failed to restore title')}\n})()"
